{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:migrate": "npx drizzle-kit migrate",
    "db:generate": "npx drizzle-kit generate"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.5",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8",
    "tsx": "^4.21.0"
  }
}
